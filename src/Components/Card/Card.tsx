import type { CardInterface } from '../../Types/CardViewModel'
import './Card.css'

interface CardProps {
  Card : CardInterface
}

export function Card( { Card } : CardProps) {
  
  return (
    <>

    </>
  )
}